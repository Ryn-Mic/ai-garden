---
title: 微调
type: concept
status: seedling
tags: [concept, finetune]
aliases: [Fine-tuning, SFT, LoRA]
---

# 微调

> [!abstract] 一句话定义
> 在预训练模型的基础上，用你自己的数据继续训练，把**行为模式**（而不只是知识）刻进权重。

## 为什么重要

当提示怎么改都稳定不下来时，说明这不是"说法"问题，而是"习惯"问题——微调是唯一解。典型场景：固定输出格式、特定领域语气、超长系统提示的替代。

## 核心原理

### 常见层次

| 方式 | 改什么 | 成本 | 适合 |
| --- | --- | --- | --- |
| 提示工程 | 什么都不改 | 最低 | 先试这个 |
| [[RAG]] | 外挂知识 | 低 | 知识更新、要引用 |
| LoRA / QLoRA | 旁路低秩矩阵 | 中 | 主流微调方式 |
| 全参数微调 | 全部权重 | 高 | 有大量数据与算力 |
| 继续预训练 | 全部权重 + 海量领域语料 | 最高 | 领域语言本身不同（如代码、法律） |
| RLHF / DPO | 对齐偏好 | 中高 | 让输出符合人类偏好 |

### LoRA 直觉

冻结原权重，在旁边挂一对小矩阵 `A·B`，只训练它们。参数量降到 0.1%–1%，效果接近全参微调，还能一个底座挂多个适配器。

### 数据比算法重要

```text
质量 > 数量。1000 条干净、一致、覆盖边界的样本，胜过 10 万条噪声。
```

关键：**一致性**。同类输入如果标注不一致，模型学到的就是噪声。

> [!warning] 别用微调做的事
> - 注入频繁变动的知识 → 用 [[RAG]]
> - 让模型"知道"最新事实 → 用 [[RAG]]
> - 修幻觉 → 微调治不了根源

## 相关节点

- 对比：[[RAG]]，选型见 [[RAG与微调]]
- 上游：[[大语言模型]]、[[Transformer]]
- 替代：[[提示工程]]
